# 시즌 카드 80종 점검 리포트 (2026-02-26)

## 1) 기본 검증 결과
- 카탈로그 카드 수: `B 40 / C 40` (총 80)
- Java 소스 `legacy` 참조: `0건`
- 카탈로그 YAML `legacy` 참조: `0건`
- 컴파일: 성공
  - classpath: `paper/season_1_end/libraries/**/*.jar`
  - source/target: `21`

## 2) 이번 반영 사항
- 레거시/호환 잔여 경로 정리
  - deprecated potion runtime(`runtime.effects`) 변환 파서 제거
  - 미사용 숫자풀 수집/표시 메서드 제거
  - dead gate(`cardsHardcodedIdHandlersEnabled`) 제거 후 실동작 경로 단순화
- 설명 파싱 안정화
  - `runtime.detail_ko`에서 ` / T2 ...` 형태를 tier-token 파싱 전 표준화
  - tier collapse 후 `20%60%` 같은 붙음 현상 방지 정규화 추가

## 3) 모호성/가독성 후보 (추가 정리 권장)
아래는 동작은 가능하지만, 플레이어 체감 설명이 모호하거나 UI 혼잡을 유발할 수 있는 문구 후보입니다.

- L272: `위치 핑(좌표/방향 안내)`
- L273: `나무 막대 우클릭 핑 재사용`
- L671: `감지 대상(레드스톤/스위치) 핑 5s`
- L1314: `희귀 블럭 탐지 핑`
- L2125: `핑 정보 왜곡(방향/거리 오차)`
- L2377: `함정/레드스톤 접근 시 본인 위치 핑 노출`
- L3050: `질주 상태 5s 유지 시 본인 위치 핑 노출`
- L3233: `오라 접근 시 본인 위치 핑 노출`
- L3363: `나무 막대 핑: 오차 좌표 저장 + 왜곡 핑 표기`
- L3364: `핑 표시 중 받는 피해`
- L3367: `핑 작동 직후 5s 공격 피해`
- L3429: `잠행 상태 유지마다 본인 위치 핑 노출`
- L3543: `좌표/핑 표기: 오버월드/엔드 모두 오차 표기`

권장 방향:
- `핑` 용어를 2개로 분리 (`방향 안내`, `위치 노출`)해 의미 고정
- 노출 효과는 `표기 대상`, `표기 지속`, `재발동 쿨` 3요소를 항상 같은 순서로 표기

## 4) 다음 점검 우선순위
1. 위 `핑` 관련 12개 라인의 표기 템플릿 통일
2. 카드별 `발동 조건` 문구를 `행동 + 상태 + 쿨타임` 순서로 통일
3. 인게임 로어(티어별 표시)에서 80개 카드 최종 수동 점검
